{
  "name": "uniswap-v3-swap",
  "version": "1.0.0",
  "description": "Simple Uniswap v3 swap using Hardhat with custom router",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "deploy-router": "hardhat run scripts/deploy-router.js --network sepolia",
    "deploy-bundler": "hardhat run scripts/deploy-bundler.js --network sepolia",
    "swap": "hardhat run scripts/swap-with-custom-router.js --network sepolia",
    "swap-bundled": "hardhat run scripts/swap-with-bundler.js --network sepolia",
    "swap-private": "hardhat run scripts/swap-private.js --network sepolia"
  },
  "keywords": [
    "uniswap",
    "v3",
    "swap",
    "hardhat",
    "defi"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0",
    "dotenv": "^16.3.1"
  }
}
